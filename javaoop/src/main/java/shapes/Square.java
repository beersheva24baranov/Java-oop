package shapes;

public class Square extends Rectangle{ // квадрат  расширяет  прямоугольник
public Square(int size) { // у квадрата 1 параметр
    super(size, size); // у прямоугольника два поэтому  наследуем

}
}